usage:

```sh
# make a virtual environment in the local directory
python3 -m venv .venv
# activate it
source .venv/bin/activate
# check that the correct `pip` is being used
which pip
# install requirements
pip install -r requirements.txt
```